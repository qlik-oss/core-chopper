import * as ex from 'excalibur';

const DEFAULT_SCALE = new ex.Vector(1.0, 1.0);

export default {
  DEFAULT_SCALE,
  BIKER_SCALE: 1.3,
  BIKER_IMPULSE: -150,
  ACCELERATION: 450,
  MAX_VELOCITY: 250,
  LEVEL_SPEED: -250,
  scale: DEFAULT_SCALE,
};
